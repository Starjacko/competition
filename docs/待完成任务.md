# 待完成任务

> 最后更新：2026-09-16
>
> 这是一张项目“作业清单”。完成一项后，要填写完成时间、代码版本和验证证据。

## 当前最重要的任务

| 优先级 | 任务 | 当前状态 | 完成条件 | 依赖/阻塞 | 证据位置 |
|---|---|---|---|---|---|
| P0 | 确认当前比赛版本和本阶段目标 | 待确认 | 明确当前要做的功能、暂时不做的功能和验收标准 | 等项目负责人确认 | `Requirements/REQ-*.md` |
| P0 | 获取公司服务器验证流程 | 待获取 | 知道提交方式、启动命令、端口传递、日志位置和结果获取方式 | 公司内部服务器规则尚未写入仓库 | 本文“服务器信息” |
| P0 | 完成本程序的服务器启动验证 | 待验证 | 公司服务器能够启动程序，判题器能够通过 HTTP 调用程序 | 需要服务器流程和判题器 | `docs/验证与测试说明.md` |
| P0 | 完成第一次真实对局 | 待验证 | 与另一名员工的程序完成一次真实对局，并保存结果 | 需要服务器安排对手 | `docs/验证与测试说明.md` |
| P1 | 建立本地自动化检查 | 进行中 | 至少能检查语法、导入、固定样例输入和响应格式 | 已新增基础单元测试，仍需增加失败回放 | `Demo/CoreGeek/tests/` |
| P1 | 为每个比赛功能建立需求记录 | 进行中 | 采集、建造、防守、任务等功能各自关联 REQ、代码和验证结果 | 基础资源与防守已建立 REQ | `Requirements/REQ-20260916-002.md` |
| P1 | 根据真实对局结果改进策略 | 待开始 | 每次改进都有对局证据、原因和下一步 | 需要真实对局结果 | `docs/验证与测试说明.md`、`docs/PROGRESS.md` |
| P2 | 补充正式技术设计和运行说明 | 进行中 | 技术文档与实际代码、服务器要求保持一致 | 当前服务器细节未知 | `docs/技术设计.md` |

## 本地可以完成的任务

- [ ] Python 语法检查。
- [ ] Python 模块导入检查。
- [ ] 固定请求样例能够得到合法 JSON 响应。
- [ ] 响应中的 `roleCommandMap` 和动作字段符合接口文档。
- [ ] 检查昼夜判断、距离计算、地图阻挡和寻路等局部逻辑。
- [ ] 检查程序能否监听传入的端口。
- [ ] 为不依赖真实对手的函数补充自动化测试。

## 只能在公司服务器完成的任务

- [ ] 确认服务器能正确启动参赛程序。
- [ ] 确认判题器能够访问 HTTP 服务。
- [ ] 确认完整比赛过程中没有超时或异常退出。
- [ ] 与另一名员工的参赛程序完成真实对局。
- [ ] 记录胜负、得分、异常次数、错误和服务器日志。
- [ ] 根据真实对局结果决定下一轮策略修改。

## 服务器信息

以下信息目前还没有写入仓库，拿到后补充。不要把密码、密钥或内部敏感地址提交到 Git。

| 信息 | 内容 |
|---|---|
| 提交流程 | 待获取 |
| 服务器地址/名称 | 待获取；敏感信息不要直接写入仓库 |
| 启动命令 | 待获取；当前接口文档示例为 `bash run.sh port` |
| 端口传递方式 | 待确认 |
| 判题器调用方式 | 待获取 |
| 日志查看方式 | 待获取 |
| 比赛结果查看方式 | 待获取 |
| 代码版本标识方式 | 待确认 |

## 完成任务时必须留下的证据

每完成一项任务，都要补充：

- 使用的代码版本或 commit ID；
- 执行过的命令；
- 命令输出或服务器结果；
- 如果是真实对局，记录对手标识、比赛编号、胜负和得分；
- 没有完成的部分、原因和下一步。

## 状态说明

- `待确认`：还不知道要做什么，不能开始实现。
- `待开始`：已经知道要做什么，但还没有开始。
- `进行中`：正在实现或整理。
- `待验证`：代码已经准备好，但还缺验证证据。
- `已完成`：已经完成规定检查，并同步了文档和进度。
- `已阻塞`：需要服务器、对手程序或用户决定才能继续。

本地检查通过不能自动变成“比赛已完成”。没有公司服务器真实对局时，比赛策略必须保持为“待服务器验证”。
