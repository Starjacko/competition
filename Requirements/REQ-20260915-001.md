# REQ-20260915-001：建立项目契约与需求关联进度闭环

## 分类与状态

- 分类：项目治理
- 状态：待澄清
- 用户确认：未确认
- 产品来源：`docs/任务书.md`
- 技术来源：`docs/接口文档.md`、`Demo/CoreGeek/pyproject.toml`、`Demo/CoreGeek/src/agent/`
- 项目总进度：`docs/PROGRESS.md`
- 决策记录：无；本次未改变架构、接口或数据模型
- 功能进度：`Progress/PROG-REQ-20260915-001-project-governance.md`

## 背景与目标

当前仓库已有比赛任务书、HTTP 接口文档和一份 Python 参赛程序，但缺少统一的项目规则、文档索引、需求台账和进度台账。本需求用于建立这些记录，让后续修改能追溯到需求、代码和验证证据。

## 已发现事实

- 比赛规则和目标以 `docs/任务书.md` 为主要产品事实来源。
- HTTP 请求、响应和动作协议以 `docs/接口文档.md`、`docs/request.txt`、`docs/response.txt` 为事实来源。
- Python 包要求 Python `>=3.11`，包配置在 `Demo/CoreGeek/pyproject.toml`。
- HTTP 服务入口为 `Demo/CoreGeek/src/agent/server.py`，决策逻辑在 `brain.py`，状态协议在 `protocol.py`，寻路在 `grid.py`。
- 当前未发现测试目录、测试文件、数据库、迁移、前端或部署脚本。

## 待确认问题

1. 当前比赛目标是完成可运行的基础参赛程序，还是已经进入某个明确的 V1 功能目标？
2. 当前要优先实现哪些比赛能力：采集、建造、防守、任务、LLM 调用，还是其他能力？
3. 判题器、启动命令和真实验收环境是否已经提供？仓库中目前只看到接口约定，未看到 `run.sh` 或判题器。
4. 是否需要为核心决策逻辑补充自动化测试和样例回放测试？

## 影响范围

本次只新增项目治理文档，不改变代码、公开接口、数据模型或运行时。后续功能需求需要根据确认后的范围，分别关联 `brain.py`、`protocol.py`、`grid.py`、`server.py` 和对应验证方式。

## 验收标准

- [x] 项目协作规则可从 `AGENTS.md` 找到。
- [x] 产品、接口、实现、需求和进度文档可从 `DOCUMENT_MAP.md` 找到。
- [x] 当前已发现的代码、配置和测试状态被如实记录。
- [ ] 用户确认当前版本目标和后续验收范围。
- [ ] 后续功能需求有独立 REQ，并关联代码变更和测试证据。

